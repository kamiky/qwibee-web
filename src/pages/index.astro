---
// Check for qwibee-creator-mode cookie and serve appropriate page
const creatorMode = Astro.cookies.get("qwibee-creator-mode")?.value === "true";

if (creatorMode) {
  return Astro.rewrite("/home/creator");
} else {
  return Astro.rewrite("/home/app");
}
---
