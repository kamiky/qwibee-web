---
// Check for qwb-creator-mode cookie and serve appropriate page
const creatorMode = Astro.cookies.get("qwb-creator-mode")?.value === "true";

if (creatorMode) {
  return Astro.rewrite("/contact/creator");
} else {
  return Astro.rewrite("/contact/app");
}
---
