---
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import LanguageSwitcher from "@/components/LanguageSwitcher.astro";
import { useTranslations } from "@/i18n/translations";

const lang = (Astro.url.searchParams.get("lang") as "en" | "fr") || "en";
const t = useTranslations(lang);
---

<Layout
  title={t.legal.creatorTermsOfSale.title}
  description={t.legal.creatorTermsOfSale.description}
  lang={lang}
>
  <Header lang={lang} />
  <LanguageSwitcher lang={lang} />

  <main class="min-h-screen bg-white">
    <div class="max-w-4xl mx-auto px-6 py-12">
      <h1 class="text-4xl font-bold text-gray-900 mb-8">
        {t.legal.creatorTermsOfSale.title}
      </h1>

      <div class="prose prose-lg max-w-none">
        <p class="text-gray-600 mb-8">
          {t.legal.creatorTermsOfSale.lastUpdated}
        </p>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section1.title}
          </h2>
          <p class="text-gray-700 leading-relaxed">
            {t.legal.creatorTermsOfSale.section1.content}
          </p>
        </section>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section2.title}
          </h2>
          <p class="text-gray-700 leading-relaxed mb-4">
            {t.legal.creatorTermsOfSale.section2.intro}
          </p>
          <ul class="list-disc pl-6 space-y-2 text-gray-700">
            <li>{t.legal.creatorTermsOfSale.section2.item1}</li>
            <li>{t.legal.creatorTermsOfSale.section2.item2}</li>
          </ul>
        </section>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section3.title}
          </h2>
          <p class="text-gray-700 leading-relaxed">
            {t.legal.creatorTermsOfSale.section3.content}
          </p>
        </section>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section4.title}
          </h2>
          <p class="text-gray-700 leading-relaxed">
            {t.legal.creatorTermsOfSale.section4.content}
          </p>
        </section>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section5.title}
          </h2>
          <p class="text-gray-700 leading-relaxed">
            {t.legal.creatorTermsOfSale.section5.content}
          </p>
        </section>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section6.title}
          </h2>
          <p class="text-gray-700 leading-relaxed">
            {t.legal.creatorTermsOfSale.section6.content}
          </p>
        </section>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section7.title}
          </h2>
          <p class="text-gray-700 leading-relaxed">
            {t.legal.creatorTermsOfSale.section7.content}
          </p>
        </section>

        <section class="mb-8">
          <h2 class="text-2xl font-semibold text-gray-900 mb-4">
            {t.legal.creatorTermsOfSale.section8.title}
          </h2>
          <p class="text-gray-700 leading-relaxed">
            {t.legal.creatorTermsOfSale.section8.content}
          </p>
        </section>
      </div>

      <div class="mt-12 pt-8 border-t border-gray-200">
        <a
          href="/"
          class="text-brand-blue-500 hover:text-brand-blue-600 font-medium"
        >
          ← {t.legal.backToHome}
        </a>
      </div>
    </div>
  </main>

  <Footer lang={lang} />
</Layout>
